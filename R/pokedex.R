#' @title Use the Pokedex Type Search Function
#' @description
#' Given a comic number, the `pokedex()` function calls the Pokemon data set and returns the name of all of the Pokemon's that have Type 1 features that the user specified in the form of a list object.
#' @export
#search_pokedex <- function(pokedex_data) {
#  matches <- tolower(pokedex_data$type_1) == tolower(type_input)
#  return(as.character(matches))
#}

#tolower !!!!
#' @examples

#type in the element they want (but are restricted only to type 1) and it lists out the Pokemon names in return

